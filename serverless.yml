service: c5-serverless-i
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs18.x

functions:
  firstfunction:
    handler: index.firsthandler
    events:
      - httpApi:
          path: /delfrinando
          method: get

  secondfunction:
    handler: index.secondhandler
    events:
      - httpApi:
          path: /pranata
          method: post

plugins:
  - serverless-offline